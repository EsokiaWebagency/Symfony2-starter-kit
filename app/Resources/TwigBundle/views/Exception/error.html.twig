{% extends "::base.html.twig" %}

{% block title -%}{{'esokia.error_occur'|trans({}, 'esokia_default')}} {{ status_text }}{% endblock title -%} 



{% block body -%}
    <h3>{{'esokia.error_occur_title'|trans({}, 'esokia_default')}}</h3>

        <div class="row">
           <div class="col-md-2">
               {{ knp_menu_render('EsokiaAdminBundle:MenuBuilder:mainMenu',   {'currentAsLink':false} ) }}
           </div>
            <div class="col-md-9">
             <h2>{{'esokia.error_server_respond'|trans({}, 'esokia_default')}} "{{ status_code }} {{ status_text }}".</h2>

                <div>
                    {{'esokia.error_server_message'|trans({}, 'esokia_default')}}
                </div>
            </div>
        </div>
{% endblock body %}

